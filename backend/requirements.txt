# FastAPI and server
fastapi
uvicorn[standard]

# Google Cloud
google-cloud-firestore
google-auth
google-auth-oauthlib
google-auth-httplib2

# Authentication
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]
httpx

# Utilities
pydantic[email]
pydantic-settings
python-dotenv
pytz

# Brokerage / portfolio data
snaptrade-python-sdk>=11.0.183

# Plaid (bank account linking + transaction sync)
plaid-python>=29.0.0

# LLM portfolio analysis
anthropic>=0.107.0

# Cost computation (import-only, hot path stays on AsyncAnthropic)
litellm>=1.40.0

# Observability
langfuse>=3.0
sentry-sdk[fastapi]>=2.18.0

# MCP server (expose SnapTrade to Claude Desktop / Code)
mcp>=1.0.0

# Testing
pytest
pytest-asyncio
pytest-cov
httpx

# Development
ruff
black